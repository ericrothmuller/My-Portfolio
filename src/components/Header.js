import React from 'react';
import Nav from './Nav';

export default function Main() {
  const linkStyle = { border: '1px black', padding: '5px' };

  return (
    <main>
        <h1>Test</h1>
        <Nav />
    </main>
  );
}
